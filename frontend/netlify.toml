# Netlify configuration file

[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/api/*"
  to = "http://localhost:8000/api/:splat" # In production, change this to your backend URL
  status = 200
  force = true
